= link_to "#{current_user ? upvote_question_path(question) : 'javascript:void(0)'}", class: "list-group-item active #{current_user ? '' : 'no-touch'}", remote: true do
  %h2.list-group-item-heading=question.votes_for
= link_to  "#{current_user ? downvote_question_path(question) : 'javascript:void(0)'}", class: "list-group-item #{current_user ? '' : 'no-touch'}", remote: true do
  %h2.list-group-item-heading= question.votes_against